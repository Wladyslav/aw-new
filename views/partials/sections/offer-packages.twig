<section class="offer-packages-wrapper">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h2 class="packages-heading">
                    Oferowane pakiety
                </h2>
            </div>
        </div>

        <div class="price-packages row">
            
                {% for package in section.single_packages %}
                <div class="single-package col-12 col-lg-3 mx-3 {% if package.best_offer %}popular-offer{% endif %}">
                    {% if package.best_offer %}<div class="single-package-best">Najczęściej wybierany</div>{% endif %}
                    <div class="package-top">Pakiet</div>
                    <div class="package-name">{{package.name}}</div>
                    <div>od</div>
                    <div class="package-price">{{package.price}}</div>
                    <p class="package-short-desc">{{package.short_desc}}</p>
                    <button class="btn btn-red popup-open">Poproś o wycenę</button>
                </div>
                {% endfor %}
           
        </div>
    </div>
</section>