{{ function('get_header', '') }}


<main id="content">
    <section class="career-wrapper container">
        <div class="page-breadcrumbs">{{ function('yoast_breadcrumb') }}</div>
        <div class="career-content-wrapper">
            <h1 class="career-header-template">{{page.post_title}}</h1>
            <div class="career-content">
                {{page.content}}
            </div>
        </div>
    </section>
</main>
{{ function('get_footer', '') }}
